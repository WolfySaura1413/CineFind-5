// CineFind configuration template
// Copy this file to config.js and fill in your keys.
// config.js is gitignored — never commit it.

window.CONFIG = {
  // Watchmode API — primary key
  WATCHMODE_API_KEY_1: "",

  // Watchmode API — fallback key (used if primary hits rate limits)
  WATCHMODE_API_KEY_2: "",

  // Default region for streaming availability (e.g., US, PT, BR)
  DEFAULT_REGION: "US",
};
